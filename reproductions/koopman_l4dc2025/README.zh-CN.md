# Koopman 生成元辨识与策略迭代

在仓库根目录调用：

```matlab
[r,folder]=demo_reproductions('koopman_l4dc2025');
```

基础MATLAB；完整数值运行约需数十秒。v0.4.1起默认写入仓库`runs/koopman_l4dc2025/`，也可用`options.outputRoot`指定父目录。旧`results/`原样保留。

[实际结果](RESULTS.md) · [全文与方程映射](../../docs/fulltext/koopman_l4dc2025.md) · [全库入口参数](../../docs/IMPLEMENTED_API.md)。保存的结果是已执行的具体变体；源码包不包含历史MAT/FIG，完整包保留这些文件。
